services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: backend
    ports:
      - "3000:3000"
    volumes:
      - ./backend:/app
      - /app/node_modules
    networks:
      - backend_network
    environment:
      REDIS_HOST : "redis"
      REDIS_PORT: "6379"
      REDIS_TTL : "3600000" # in milliseconds (1 hour)
      PORT : "3000"

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    container_name: frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    networks:
      - backend_network
    environment:
      PORT: "5173"
      VITE_LOG_LEVEL: "debug"

  nginx:
    build: ./nginx
    ports: 
      - "80:80"
    networks:
      - backend_network
  redis:
    image: redis:latest
    container_name: redis
    ports:
      - "6379:6379"
    # volumes:
    #   - redis_data:/data
    networks:
      - backend_network
    command: ["redis-server", "--appendonly", "yes"]

# volumes:
#   redis_data:

networks:
  backend_network:
    driver: bridge